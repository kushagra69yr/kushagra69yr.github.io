const m=document.getElementById('matrix'),x=m.getContext('2d');let w,h,d;const chars='01{}[]<>/\\=+*#@$AI_ML_CODE';function resize(){w=m.width=innerWidth;h=m.height=innerHeight;d=Array(Math.ceil(w/15)).fill(0).map(()=>Math.random()*-80)}function rain(){x.fillStyle='rgba(2,5,8,.075)';x.fillRect(0,0,w,h);x.font='11px monospace';d.forEach((v,i)=>{x.fillStyle=Math.random()>.9?'rgba(99,233,255,.9)':'rgba(82,160,175,.25)';x.fillText(chars[Math.random()*chars.length|0],i*15,v*15);if(v*15>h&&Math.random()>.975)d[i]=0;d[i]++});requestAnimationFrame(rain)}addEventListener('resize',resize);resize();rain();const n=document.getElementById('network'),c=n.getContext('2d');let pts=[];function ns(){n.width=innerWidth;n.height=innerHeight;pts=Array.from({length:Math.min(75,innerWidth/18)},()=>({x:Math.random()*n.width,y:Math.random()*n.height,vx:(Math.random()-.5)*.3,vy:(Math.random()-.5)*.3}))}function network(){c.clearRect(0,0,n.width,n.height);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>n.width)p.vx*=-1;if(p.y<0||p.y>n.height)p.vy*=-1;c.fillStyle='rgba(99,233,255,.55)';c.beginPath();c.arc(p.x,p.y,1.1,0,7);c.fill()});for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){let a=pts[i],b=pts[j],q=Math.hypot(a.x-b.x,a.y-b.y);if(q<145){c.strokeStyle=`rgba(99,210,230,${.1*(1-q/145)})`;c.beginPath();c.moveTo(a.x,a.y);c.lineTo(b.x,b.y);c.stroke()}}requestAnimationFrame(network)}addEventListener('resize',ns);ns();network();